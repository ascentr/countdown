version: "3.9"
services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - 8000:8000
    # image: app:countdown
    image: countdown:latest
    container_name: countdown_container
    working_dir: /app
    command: python manage.py runserver 0.0.0.0:8000
